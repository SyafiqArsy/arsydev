// TODO: Sprint berikutnya — Registrasi plugin GSAP.
// File ini akan digunakan untuk melakukan registrasi plugin GSAP
// seperti ScrollTrigger, TextPlugin, dll. menggunakan gsap.registerPlugin(...).
// Saat ini masih placeholder karena engine animasi masih menggunakan Framer Motion.
//
// Contoh nantinya:
// import { gsap } from "gsap";
// import { ScrollTrigger } from "gsap/ScrollTrigger";
// gsap.registerPlugin(ScrollTrigger);
// export { gsap, ScrollTrigger };

export {};
